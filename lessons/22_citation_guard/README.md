# 第 22 课：引用来源约束

本课给 RAG 增加一个可检查的引用契约：

1. Prompt 要求回答末尾输出 `参考来源：`。
2. 生成后解析回答中的文件名。
3. 检查引用是否真实存在于本次检索结果。
4. 区分“缺少引用”“引用了未检索文档”等问题。

运行：

```bash
python lessons/22_citation_guard/citation_guard.py
```

这不是证明答案绝对正确，而是把来源要求从自然语言约定变成可自动检查的程序规则。
