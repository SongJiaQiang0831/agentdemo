# 第 19 课：RAG 端到端质量评估

前面评估了“是否检索到正确文档”。本课继续检查模型回答是否覆盖关键事实，并把检索排名、回答事实覆盖率和延迟放在同一张报告里。

运行：

```bash
python lessons/19_rag_quality_eval/evaluate_quality.py
```

本课指标是教学用基线，不等同于完整人工评测：事实覆盖率不能完全判断答案是否忠实，也不能发现所有幻觉。
