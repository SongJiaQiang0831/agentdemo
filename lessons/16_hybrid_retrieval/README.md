# 第 16 课：混合检索与重排序

本案例在已有向量检索之上增加一个轻量级关键词检索器，并将两种信号融合后重新排序。

- 向量检索：理解问题的语义相似性
- 关键词检索：命中术语、函数名和专有名词
- 混合检索：合并两种排序信号
- 重排序（Rerank）：按综合分数重新选择前 K 个片段

运行前确保已经建立索引：

```bash
python build_index.py
python lessons/16_hybrid_retrieval/hybrid_rag.py
```

本课不修改根目录 `rag.py` 的默认行为，便于和前面课程对比。
